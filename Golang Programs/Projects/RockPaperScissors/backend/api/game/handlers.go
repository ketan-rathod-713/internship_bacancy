package game

import "github.com/gofiber/fiber/v2"

/* give paginated response and frontend will do load more or just do pagination. order by time stamp. */
func (a *api) GetGamesPlayedByUser(ctx *fiber.Ctx) error {

	return nil
}
